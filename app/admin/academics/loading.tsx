import { DashboardRouteLoading } from '@/components/ui/dashboard-route-loading';

export default function LoadingAcademicsPage() {
  return <DashboardRouteLoading title="Academics" hint="Loading classes, subjects, exams, and result controls..." />;
}
