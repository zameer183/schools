import { DashboardRouteLoading } from '@/components/ui/dashboard-route-loading';

export default function LoadingMessagesPage() {
  return <DashboardRouteLoading title="Messages" hint="Loading inbox, sent items, and recipient lists..." />;
}
