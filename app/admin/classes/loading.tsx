import { DashboardRouteLoading } from '@/components/ui/dashboard-route-loading';

export default function LoadingClassesPage() {
  return <DashboardRouteLoading title="Classes" hint="Loading class cards, subjects, and teacher assignments..." />;
}
